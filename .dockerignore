**/node_modules
**/.venv
**/__pycache__
backend/static
.git
*.md
.env
logs
cookies.txt
# MTProto auth key: must never end up in an image layer (layers are shipped
# and cached). It is bind-mounted at runtime instead — see docker-compose.yml.
.secrets
**/.secrets
*.session
*.session-journal
