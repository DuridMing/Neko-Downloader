# local config (cp .env.example .env)
.env

# dependencies / build artifacts
node_modules/
.venv/
__pycache__/
backend/static/

# runtime data
logs/
cookies.txt

# Telegram MTProto session = full account access. Never commit, never back up
# with app data. Created 0600 by scripts/telegram-login.py.
.secrets/
*.session
*.session-journal

# misc
*.log
.DS_Store
.pytest_cache/
# Scratch: broken/oversized download artifacts kept while debugging a case.
/test/
# Stray Telegram DC cache written by an older experiment (public keys only).
/.mtproto